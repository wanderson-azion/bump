name: simple

on:
  push:
    branches: ["master"]

jobs:
  bump:
    runs-on: ubuntu-16.04
    steps:
      - uses: actions/checkout@v2
      - name: Bump Version
        run: |
          git rev-parse --short HEAD
